name: OpenChamber

on:
  workflow_dispatch:
    inputs:
      tunnel_provider:
        description: "Select Tunnel Provider"
        required: true
        default: "ngrok"
        type: choice
        options:
          - ngrok
          - cloudflare
      timeout_minutes:
        description: "Auto-shutdown after (minutes)"
        required: true
        default: "300"
        type: string

permissions:
  actions: write
  contents: read

jobs:
  serve:
    runs-on: ubuntu-latest
    env:
      EVOLVEX_API_KEY: ${{ secrets.EVOLVEX_API_KEY }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Git with Your Credentials
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Clone All Your Repos
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
        run: |
          mkdir -p "$HOME/repos"
          cd "$HOME/repos"

          repos=$(curl -fsSL \
            -H "Authorization: token $GH_TOKEN" \
            "https://api.github.com/user/repos?per_page=100&affiliation=owner" \
            | jq -r '.[].clone_url')

          for repo in $repos; do
            echo "Cloning $repo..."
            git clone "https://$GH_TOKEN@${repo#https://}" || true
          done

      # ============================================================
      # PERSISTENCE: Restore previous session (OAuth tokens, accounts)
      # ============================================================
      - name: Restore Session Data (Artifact)
        uses: dawidd6/action-download-artifact@v6
        continue-on-error: true
        with:
          name: opencode-session
          path: /tmp/opencode-restore
          workflow: opencode.yml
          workflow_conclusion: success
          if_no_artifact_found: ignore
          repo: ${{ github.repository }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply Restored Session Data
        run: |
          echo "=== RESTORE DEBUG ==="
          echo "Contents of /tmp/opencode-restore:"
          ls -la /tmp/opencode-restore/ 2>/dev/null || echo "Directory does not exist"

          if [ -d "/tmp/opencode-restore" ]; then
            echo "Found restore directory, checking contents..."
            find /tmp/opencode-restore -type f 2>/dev/null || echo "No files found"

            mkdir -p "$HOME/.config/opencode" \
                     "$HOME/.local/share/opencode/project" \
                     "$HOME/.local/state/opencode" \
                     "$HOME/.config/openchamber" \
                     "$HOME/.config/opencode-worktree" \
                     "$HOME/.local/share/opencode/worktree" \
                     "$HOME/.local/share/opencode/bin" \
                     "$HOME/.local/share/opencode/log" \
                     "$HOME/.cache/opencode"

            if [ -d "/tmp/opencode-restore/config" ]; then
              echo "Restoring OpenCode config files..."
              cp -rv /tmp/opencode-restore/config/* "$HOME/.config/opencode/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/openchamber" ]; then
              echo "Restoring OpenChamber config files..."
              cp -rv /tmp/opencode-restore/openchamber/* "$HOME/.config/openchamber/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/opencode-worktree" ]; then
              echo "Restoring Worktree config files..."
              cp -rv /tmp/opencode-restore/opencode-worktree/* "$HOME/.config/opencode-worktree/" 2>/dev/null || true
            fi

            if [ -f "/tmp/opencode-restore/auth.json" ]; then
              echo "Restoring auth.json..."
              cp -v /tmp/opencode-restore/auth.json "$HOME/.local/share/opencode/auth.json" 2>/dev/null || true
            fi

            if [ -f "/tmp/opencode-restore/opencode.db" ]; then
              echo "Restoring SQLite database..."
              cp -v /tmp/opencode-restore/opencode.db* "$HOME/.local/share/opencode/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/state" ]; then
              echo "Restoring state directory..."
              cp -rv /tmp/opencode-restore/state/* "$HOME/.local/state/opencode/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/project" ]; then
              echo "Restoring project sessions..."
              cp -rv /tmp/opencode-restore/project/* "$HOME/.local/share/opencode/project/" 2>/dev/null || true
              echo "Sessions restored: $(find "$HOME/.local/share/opencode/project" -type f 2>/dev/null | wc -l) files"
            fi

            if [ -d "/tmp/opencode-restore/worktree" ]; then
              echo "Restoring git worktrees..."
              cp -rv /tmp/opencode-restore/worktree/* "$HOME/.local/share/opencode/worktree/" 2>/dev/null || true
              echo "Worktrees restored: $(find "$HOME/.local/share/opencode/worktree" -type f 2>/dev/null | wc -l) files"
            fi

            if [ -d "/tmp/opencode-restore/cache" ]; then
              echo "Restoring cache..."
              cp -rv /tmp/opencode-restore/cache/* "$HOME/.cache/opencode/" 2>/dev/null || true
              echo "Cache restored: $(find "$HOME/.cache/opencode" -type f 2>/dev/null | wc -l) files"
            fi

            if [ -d "/tmp/opencode-restore/log" ]; then
              echo "Restoring logs..."
              cp -rv /tmp/opencode-restore/log/* "$HOME/.local/share/opencode/log/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/bin" ]; then
              echo "Restoring bin directory..."
              cp -rv /tmp/opencode-restore/bin/* "$HOME/.local/share/opencode/bin/" 2>/dev/null || true
            fi

            if [ -d "/tmp/opencode-restore/storage" ]; then
              echo "Restoring legacy storage..."
              mkdir -p "$HOME/.local/share/opencode/storage"
              cp -rv /tmp/opencode-restore/storage/* "$HOME/.local/share/opencode/storage/" 2>/dev/null || true
            fi

            echo "Session restored successfully!"
          else
            echo "No previous session found. Fresh start."
          fi


      - name: Restore Oh My OpenAgent Data
        run: |
          set -euo pipefail
          echo "Restoring Oh My OpenAgent / Oh My OpenCode files..."
          mkdir -p "$HOME/.config/opencode" \
                   "$HOME/.config/oh-my-openagent" \
                   "$HOME/.config/oh-my-opencode" \
                   "$HOME/.local/share/oh-my-openagent" \
                   "$HOME/.local/share/oh-my-opencode" \
                   "$HOME/.cache/oh-my-openagent" \
                   "$HOME/.cache/oh-my-opencode" \
                   "$HOME/.oh-my-openagent" \
                   "$HOME/.oh-my-opencode"

          [ -d "/tmp/opencode-restore/opencode-omo-config" ] && cp -rv /tmp/opencode-restore/opencode-omo-config/* "$HOME/.config/opencode/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-config-openagent" ] && cp -rv /tmp/opencode-restore/omo-config-openagent/* "$HOME/.config/oh-my-openagent/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-config-opencode" ] && cp -rv /tmp/opencode-restore/omo-config-opencode/* "$HOME/.config/oh-my-opencode/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-share-openagent" ] && cp -rv /tmp/opencode-restore/omo-share-openagent/* "$HOME/.local/share/oh-my-openagent/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-share-opencode" ] && cp -rv /tmp/opencode-restore/omo-share-opencode/* "$HOME/.local/share/oh-my-opencode/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-cache-openagent" ] && cp -rv /tmp/opencode-restore/omo-cache-openagent/* "$HOME/.cache/oh-my-openagent/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-cache-opencode" ] && cp -rv /tmp/opencode-restore/omo-cache-opencode/* "$HOME/.cache/oh-my-opencode/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-home-openagent" ] && cp -rv /tmp/opencode-restore/omo-home-openagent/* "$HOME/.oh-my-openagent/" 2>/dev/null || true
          [ -d "/tmp/opencode-restore/omo-home-opencode" ] && cp -rv /tmp/opencode-restore/omo-home-opencode/* "$HOME/.oh-my-opencode/" 2>/dev/null || true
          if [ -d "/tmp/opencode-restore/repo-omo-configs" ]; then
            (cd /tmp/opencode-restore/repo-omo-configs && tar -cf - .) | (cd "$HOME/repos" && tar -xf -) 2>/dev/null || true
          fi

      # ============================================================
      # CACHE: Speed up npm installations
      # ============================================================
      - name: Cache npm modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
          key: npm-cache-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-cache-${{ runner.os }}-

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Tools
        run: |
          npm install -g opencode-ai @openchamber/web oh-my-openagent oh-my-opencode
          sudo apt update
          sudo apt install -y jq lsof coreutils curl wget

          if [[ "${{ github.event.inputs.tunnel_provider }}" == "cloudflare" ]]; then
            wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
            sudo dpkg -i cloudflared-linux-amd64.deb
          else
            curl -fsSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
            echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
            sudo apt update
            sudo apt install -y ngrok
            ngrok config add-authtoken "${{ secrets.NGROK_AUTH_TOKEN }}"
          fi

      - name: Configure OpenCode (Complete Antigravity Suite)
        run: |
          mkdir -p "$HOME/.config/opencode"
          cat << 'EOF' > "$HOME/.config/opencode/opencode.json"
          {
            "$schema": "https://opencode.ai/config.json",
            "plugin": ["opencode-antigravity-auth@beta", "oh-my-openagent"],
            "provider": {
              "openai": {
                "options": {
                  "baseURL": "https://anyrouter.top/v1",
                  "apiKey": "sk-Ntp7Be3U7aRQjtNyLUJReKeO6wuInQrnha5VaL8wvmxOf6pb",
                  "store": true,
                  "include": [],
                  "reasoningSummary": "detailed"
                }
              },
              "evolvex": {
                "npm": "@ai-sdk/openai-compatible",
                "name": "Modal",
                "options": {
                  "baseURL": "https://api.us-west-2.modal.direct/v1",
                  "apiKey": "{env:EVOLVEX_API_KEY}"
                },
                "models": {
                  "zai-org/GLM-5.1-FP8": {
                    "name": "GLM-5.1"
                  }
                }
              },
              "google": {
                "models": {
                  "antigravity-gemini-3-pro": {
                    "name": "Gemini 3 Pro (Antigravity)",
                    "limit": { "context": 1048576, "output": 65535 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] },
                    "variants": {
                      "low": { "thinkingLevel": "low" },
                      "high": { "thinkingLevel": "high" }
                    }
                  },
                  "antigravity-gemini-3-flash": {
                    "name": "Gemini 3 Flash (Antigravity)",
                    "limit": { "context": 1048576, "output": 65536 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] },
                    "variants": {
                      "minimal": { "thinkingLevel": "minimal" },
                      "low": { "thinkingLevel": "low" },
                      "medium": { "thinkingLevel": "medium" },
                      "high": { "thinkingLevel": "high" }
                    }
                  },
                  "antigravity-claude-sonnet-4-5": {
                    "name": "Claude Sonnet 4.5 (no thinking) (Antigravity)",
                    "limit": { "context": 200000, "output": 64000 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] }
                  },
                  "antigravity-claude-sonnet-4-5-thinking": {
                    "name": "Claude Sonnet 4.5 Thinking (Antigravity)",
                    "limit": { "context": 200000, "output": 64000 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] },
                    "variants": {
                      "low": { "thinkingConfig": { "thinkingBudget": 8192 } },
                      "max": { "thinkingConfig": { "thinkingBudget": 32768 } }
                    }
                  },
                  "antigravity-claude-opus-4-5-thinking": {
                    "name": "Claude Opus 4.5 Thinking (Antigravity)",
                    "limit": { "context": 200000, "output": 64000 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] },
                    "variants": {
                      "low": { "thinkingConfig": { "thinkingBudget": 8192 } },
                      "max": { "thinkingConfig": { "thinkingBudget": 32768 } }
                    }
                  },
                  "gemini-2.5-flash": {
                    "name": "Gemini 2.5 Flash (Gemini CLI)",
                    "limit": { "context": 1048576, "output": 65536 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] }
                  },
                  "gemini-2.5-pro": {
                    "name": "Gemini 2.5 Pro (Gemini CLI)",
                    "limit": { "context": 1048576, "output": 65536 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] }
                  },
                  "gemini-3-flash-preview": {
                    "name": "Gemini 3 Flash Preview (Gemini CLI)",
                    "limit": { "context": 1048576, "output": 65536 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] }
                  },
                  "gemini-3-pro-preview": {
                    "name": "Gemini 3 Pro Preview (Gemini CLI)",
                    "limit": { "context": 1048576, "output": 65535 },
                    "modalities": { "input": ["text", "image", "pdf"], "output": ["text"] }
                  }
                }
              }
            }
          }
          EOF

      - name: Normalize Agent Model and Temperature Overrides
        run: |
          set -euo pipefail
          echo "Removing explicit model/temperature overrides from OpenCode and OmO agent configs. Provider config is not rewritten."
          mkdir -p "$HOME/.config/opencode"

          # Keep OmO registered, but make the user config contain no per-agent model or sampling overrides.
          # oh-my-opencode.* is written too because OmO currently checks the legacy basename first when both exist.
          cat > "$HOME/.config/opencode/oh-my-opencode.jsonc" <<'EOF'
          {
            "model_fallback": false,
            "disabled_hooks": ["runtime-fallback"],
            "agents": {
              "sisyphus": {},
              "hephaestus": {},
              "prometheus": {},
              "oracle": {},
              "librarian": {},
              "explore": {},
              "multimodal-looker": {},
              "metis": {},
              "momus": {},
              "atlas": {},
              "sisyphus-junior": {}
            }
          }
          EOF
          cp "$HOME/.config/opencode/oh-my-opencode.jsonc" "$HOME/.config/opencode/oh-my-openagent.jsonc"

          python3 <<'PY'
          from __future__ import annotations
          from pathlib import Path
          import json, re, os

          HOME = Path.home()
          REPOS = HOME / "repos"
          STRIP_KEYS = {"model", "fallback_models", "fallbackModels", "temperature", "top_p", "topP"}
          CONTAINER_KEYS = {"agent", "agents", "command", "commands", "categories"}

          def strip_jsonc_comments(s: str) -> str:
              out = []
              i = 0
              in_str = False
              quote = ""
              esc = False
              while i < len(s):
                  c = s[i]
                  n = s[i+1] if i + 1 < len(s) else ""
                  if in_str:
                      out.append(c)
                      if esc:
                          esc = False
                      elif c == "\\":
                          esc = True
                      elif c == quote:
                          in_str = False
                      i += 1
                      continue
                  if c in ('"', "'"):
                      in_str = True
                      quote = c
                      out.append(c)
                      i += 1
                      continue
                  if c == "/" and n == "/":
                      while i < len(s) and s[i] not in "\r\n":
                          i += 1
                      out.append("\n")
                      continue
                  if c == "/" and n == "*":
                      i += 2
                      while i + 1 < len(s) and not (s[i] == "*" and s[i+1] == "/"):
                          i += 1
                      i += 2
                      continue
                  out.append(c)
                  i += 1
              return "".join(out)

          def parse_jsonc(text: str):
              clean = strip_jsonc_comments(text)
              clean = re.sub(r",\s*([}\]])", r"\1", clean)
              return json.loads(clean)

          def strip_agentish(obj, active=False):
              changed = False
              if isinstance(obj, dict):
                  if active:
                      for k in list(obj.keys()):
                          if k in STRIP_KEYS:
                              del obj[k]
                              changed = True
                  for k, v in list(obj.items()):
                      child_active = active or k in CONTAINER_KEYS
                      if strip_agentish(v, child_active):
                          changed = True
              elif isinstance(obj, list):
                  for v in obj:
                      if strip_agentish(v, active):
                          changed = True
              return changed

          def rewrite_jsonc_file(path: Path):
              try:
                  raw = path.read_text(errors="ignore")
                  data = parse_jsonc(raw)
              except Exception:
                  return False
              changed = strip_agentish(data, False)
              if changed:
                  path.write_text(json.dumps(data, indent=2, ensure_ascii=False) + "\n")
                  print(f"stripped agent model/temp keys: {path}")
              return changed

          def strip_frontmatter(path: Path):
              try:
                  text = path.read_text(errors="ignore")
              except Exception:
                  return False
              if not text.startswith("---"):
                  return False
              parts = text.split("---", 2)
              if len(parts) < 3:
                  return False
              fm = parts[1]
              body = parts[2]
              lines = fm.splitlines(True)
              new = []
              changed = False
              i = 0
              key_re = re.compile(r"^(\s*)(model|fallback_models|fallbackModels|temperature|top_p|topP)\s*:")
              while i < len(lines):
                  line = lines[i]
                  m = key_re.match(line)
                  if not m:
                      new.append(line)
                      i += 1
                      continue
                  changed = True
                  indent = len(m.group(1))
                  i += 1
                  # If the removed key has a block value, remove indented continuation lines too.
                  while i < len(lines):
                      nxt = lines[i]
                      if not nxt.strip():
                          i += 1
                          continue
                      cur_indent = len(nxt) - len(nxt.lstrip(" "))
                      if cur_indent > indent:
                          i += 1
                          continue
                      break
              if changed:
                  path.write_text("---" + "".join(new) + "---" + body)
                  print(f"stripped agent frontmatter: {path}")
              return changed

          json_paths = set()
          candidates = [
              HOME / ".config/opencode",
              HOME / ".config/oh-my-openagent",
              HOME / ".config/oh-my-opencode",
              HOME / ".local/share/oh-my-openagent",
              HOME / ".local/share/oh-my-opencode",
              HOME / ".cache/oh-my-openagent",
              HOME / ".cache/oh-my-opencode",
              HOME / ".oh-my-openagent",
              HOME / ".oh-my-opencode",
          ]
          for base in candidates:
              if base.exists():
                  for pat in ("opencode.json", "opencode.jsonc", "oh-my-openagent.json", "oh-my-openagent.jsonc", "oh-my-opencode.json", "oh-my-opencode.jsonc"):
                      json_paths.update(base.rglob(pat))
          if REPOS.exists():
              for pat in ("**/opencode.json", "**/opencode.jsonc", "**/.opencode/oh-my-openagent.json", "**/.opencode/oh-my-openagent.jsonc", "**/.opencode/oh-my-opencode.json", "**/.opencode/oh-my-opencode.jsonc"):
                  json_paths.update(REPOS.glob(pat))

          for p in sorted(json_paths):
              rewrite_jsonc_file(p)

          md_paths = set()
          md_dirs = [HOME / ".config/opencode/agents", HOME / ".config/opencode/agent", HOME / ".config/opencode/commands", HOME / ".config/opencode/command"]
          if REPOS.exists():
              for pat in ("**/.opencode/agents", "**/.opencode/agent", "**/.opencode/commands", "**/.opencode/command"):
                  md_dirs.extend(REPOS.glob(pat))
          for d in md_dirs:
              if d.exists():
                  md_paths.update(d.rglob("*.md"))
          for p in sorted(md_paths):
              strip_frontmatter(p)
          PY

          # Best-effort local package patch. OmO has built-in model chains; empty config alone can still leave
          # models visible in the OpenCode agent editor. This changes only local runner package files, not GitHub.
          python3 <<'PY'
          from pathlib import Path
          import os, re, subprocess
          HOME = Path.home()
          roots = []
          try:
              npm_root = subprocess.check_output(["npm", "root", "-g"], text=True).strip()
              roots.append(Path(npm_root))
          except Exception:
              pass
          roots += [
              HOME / ".config/opencode/node_modules",
              HOME / ".local/share/opencode/bin/node_modules",
              HOME / ".cache/opencode",
              HOME / ".npm/_npx",
          ]
          packages = []
          for root in roots:
              if not root.exists():
                  continue
              for name in ("oh-my-openagent", "oh-my-opencode"):
                  packages.extend(root.rglob(name))
          seen = set()
          packages = [p for p in packages if p.is_dir() and not (str(p) in seen or seen.add(str(p)))]
          signals = [
              "Sisyphus", "THE LIBRARIAN", "librarian", "claude-opus-4-7", "gpt-5.4-mini-fast",
              "fallback_models", "runtime-fallback", "visual-engineering", "ultrabrain"
          ]
          js_files = []
          data_files = []
          for pkg in packages:
              for p in pkg.rglob("*"):
                  if not p.is_file():
                      continue
                  if p.suffix in (".js", ".mjs", ".cjs", ".ts"):
                      js_files.append(p)
                  elif p.suffix in (".json", ".jsonc", ".md", ".yaml", ".yml"):
                      data_files.append(p)
          changed = 0
          for p in js_files:
              try:
                  s = p.read_text(errors="ignore")
              except Exception:
                  continue
              if not any(sig in s for sig in signals):
                  continue
              orig = s
              # Set hard-coded agent/category models and sampling params to undefined without touching providers.
              s = re.sub(r'(?<![\w$])model\s*:\s*["\'][^"\']+["\']', 'model: undefined', s)
              s = re.sub(r'(["\'])model\1\s*:\s*["\'][^"\']+["\']', r'\1model\1: undefined', s)
              s = re.sub(r'(?<![\w$])temperature\s*:\s*-?\d+(?:\.\d+)?', 'temperature: undefined', s)
              s = re.sub(r'(["\'])temperature\1\s*:\s*-?\d+(?:\.\d+)?', r'\1temperature\1: undefined', s)
              s = re.sub(r'(?<![\w$])top_p\s*:\s*-?\d+(?:\.\d+)?', 'top_p: undefined', s)
              s = re.sub(r'(["\'])top_p\1\s*:\s*-?\d+(?:\.\d+)?', r'\1top_p\1: undefined', s)
              s = re.sub(r'(?<![\w$])topP\s*:\s*-?\d+(?:\.\d+)?', 'topP: undefined', s)
              s = re.sub(r'(["\'])topP\1\s*:\s*-?\d+(?:\.\d+)?', r'\1topP\1: undefined', s)
              s = re.sub(r'(?<![\w$])fallback_models\s*:\s*\[[\s\S]*?\]', 'fallback_models: []', s)
              s = re.sub(r'(?<![\w$])fallbackModels\s*:\s*\[[\s\S]*?\]', 'fallbackModels: []', s)
              if s != orig:
                  p.write_text(s)
                  changed += 1
                  print(f"patched local OmO package defaults: {p}")
          print(f"Local OmO package files patched: {changed}")
          PY

          echo "Remaining explicit OpenCode/OmO agent model or temperature keys in saved config files:"
          {
            find "$HOME/.config/opencode" "$HOME/.config/oh-my-openagent" "$HOME/.config/oh-my-opencode" "$HOME/.local/share/oh-my-openagent" "$HOME/.local/share/oh-my-opencode" "$HOME/.cache/oh-my-openagent" "$HOME/.cache/oh-my-opencode" "$HOME/.oh-my-openagent" "$HOME/.oh-my-opencode" -type f 2>/dev/null
            find "$HOME/repos" -type f 2>/dev/null
          } | grep -E '(opencode\.jsonc?$|oh-my-openagent\.jsonc?$|oh-my-opencode\.jsonc?$|/agents?/.*\.md$|/commands?/.*\.md$)' \
            | while read -r f; do
                if grep -nE '^[[:space:]]*("?)(model|fallback_models|fallbackModels|temperature|top_p|topP)\1[[:space:]]*:' "$f" >/dev/null 2>&1; then
                  echo "WARN: $f still has direct agent/model-like key(s)"
                  grep -nE '^[[:space:]]*("?)(model|fallback_models|fallbackModels|temperature|top_p|topP)\1[[:space:]]*:' "$f" | sed 's/:.*$/: <redacted>/' || true
                fi
              done

      - name: Initial Startup
        run: |
          nohup stdbuf -oL openchamber --port 3000 > openchamber.log 2>&1 &

          echo "Waiting for OpenChamber to start..."
          for i in $(seq 1 30); do
            if lsof -iTCP:3000 -sTCP:LISTEN >/dev/null 2>&1; then
              echo "OpenChamber is listening on port 3000"
              break
            fi
            sleep 2
          done

          if ! lsof -iTCP:3000 -sTCP:LISTEN >/dev/null 2>&1; then
            echo "OpenChamber failed to start"
            echo "=== openchamber.log ==="
            tail -n 200 openchamber.log || true
            exit 1
          fi

          : > tunnel.log

          if [[ "${{ github.event.inputs.tunnel_provider }}" == "ngrok" ]]; then
            nohup stdbuf -oL ngrok http 3000 --log=stdout > tunnel.log 2>&1 &

            echo "Waiting for ngrok URL..."
            for i in $(seq 1 30); do
              URL=$(curl -fsSL http://127.0.0.1:4040/api/tunnels 2>/dev/null | jq -r '.tunnels[0].public_url // empty')
              if [ -n "$URL" ]; then
                echo "URL=$URL" >> "$GITHUB_ENV"
                echo "Tunnel ready: $URL"
                break
              fi

              if ! pgrep -f "ngrok http 3000" >/dev/null 2>&1; then
                echo "ngrok exited early"
                echo "=== tunnel.log ==="
                tail -n 200 tunnel.log || true
                exit 1
              fi

              sleep 2
            done
          else
            nohup stdbuf -oL cloudflared tunnel --no-autoupdate --url http://127.0.0.1:3000 > tunnel.log 2>&1 &

            echo "Waiting for Cloudflare URL..."
            for i in $(seq 1 45); do
              URL=$(grep -oE 'https://[a-z0-9-]+\.trycloudflare\.com' tunnel.log | tail -n 1 || true)
              if [ -n "$URL" ]; then
                echo "URL=$URL" >> "$GITHUB_ENV"
                echo "Tunnel ready: $URL"
                break
              fi

              if ! pgrep -f "cloudflared tunnel --no-autoupdate --url http://127.0.0.1:3000" >/dev/null 2>&1; then
                echo "cloudflared exited early"
                echo "=== tunnel.log ==="
                tail -n 200 tunnel.log || true
                exit 1
              fi

              sleep 2
            done
          fi

          if ! grep -q '^URL=' "$GITHUB_ENV"; then
            echo "Tunnel failed to produce a public URL"
            echo "=== openchamber.log ==="
            tail -n 200 openchamber.log || true
            echo "=== tunnel.log ==="
            tail -n 200 tunnel.log || true
            exit 1
          fi

      - name: Monitor & Self-Heal
        run: |
          echo "------------------------------------------"
          echo "SERVER LIVE AT: ${{ env.URL }}"
          echo "AUTO-SHUTDOWN IN: ${{ github.event.inputs.timeout_minutes }} minute(s)"
          echo "All your repos available at: ~/repos/"
          echo "------------------------------------------"

          START_TIME=$(date +%s)
          TIMEOUT_SECONDS=$(( ${{ github.event.inputs.timeout_minutes }} * 60 ))

          while true; do
            ELAPSED=$(( $(date +%s) - START_TIME ))
            REMAINING=$(( TIMEOUT_SECONDS - ELAPSED ))

            if [ "$REMAINING" -le 0 ]; then
              echo "[$(date)] Timeout reached. Initiating graceful shutdown..."
              pkill -f "openchamber|ngrok|cloudflared" 2>/dev/null || true
              break
            fi

            if [ $(( ELAPSED % 300 )) -lt 5 ]; then
              echo "[$(date)] Time remaining: $(( REMAINING / 60 )) minutes"
            fi

            if ! lsof -iTCP:3000 -sTCP:LISTEN >/dev/null 2>&1; then
              echo "[$(date)] Restoring OpenChamber..."
              nohup stdbuf -oL openchamber --port 3000 >> openchamber.log 2>&1 &
              sleep 10
            fi

            if [[ "${{ github.event.inputs.tunnel_provider }}" == "ngrok" ]]; then
              if ! pgrep -f "ngrok http 3000" >/dev/null 2>&1; then
                echo "[$(date)] Re-hosting ngrok tunnel..."
                : > tunnel.log
                nohup stdbuf -oL ngrok http 3000 --log=stdout > tunnel.log 2>&1 &
                sleep 10
                URL=$(curl -fsSL http://127.0.0.1:4040/api/tunnels 2>/dev/null | jq -r '.tunnels[0].public_url // empty')
                [ -n "$URL" ] && echo "NEW LINK: $URL"
              fi
            else
              if ! pgrep -f "cloudflared tunnel --no-autoupdate --url http://127.0.0.1:3000" >/dev/null 2>&1; then
                echo "[$(date)] Re-hosting Cloudflare tunnel..."
                : > tunnel.log
                nohup stdbuf -oL cloudflared tunnel --no-autoupdate --url http://127.0.0.1:3000 > tunnel.log 2>&1 &
                sleep 15
                URL=$(grep -oE 'https://[a-z0-9-]+\.trycloudflare\.com' tunnel.log | tail -n 1 || true)
                [ -n "$URL" ] && echo "NEW LINK: $URL"
              fi
            fi

            tail -n 1 openchamber.log 2>/dev/null || true
            sleep 5
          done

      - name: Prepare Session Data for Save
        if: always()
        run: |
          echo "Preparing session data for persistence..."
          mkdir -p /tmp/opencode-save/config \
                   /tmp/opencode-save/openchamber \
                   /tmp/opencode-save/opencode-worktree \
                   /tmp/opencode-save/project \
                   /tmp/opencode-save/worktree \
                   /tmp/opencode-save/state \
                   /tmp/opencode-save/storage \
                   /tmp/opencode-save/cache \
                   /tmp/opencode-save/log \
                   /tmp/opencode-save/bin

          if [ -d "$HOME/.config/opencode" ] && [ "$(ls -A "$HOME/.config/opencode" 2>/dev/null)" ]; then
            cp -rv "$HOME/.config/opencode/"* /tmp/opencode-save/config/ 2>/dev/null || true
            echo "OpenCode config saved: $(ls -la /tmp/opencode-save/config/ 2>/dev/null | wc -l) files"
          else
            echo "No OpenCode config data to save"
          fi

          if [ -d "$HOME/.config/openchamber" ] && [ "$(ls -A "$HOME/.config/openchamber" 2>/dev/null)" ]; then
            cp -rv "$HOME/.config/openchamber/"* /tmp/opencode-save/openchamber/ 2>/dev/null || true
            echo "OpenChamber config saved: $(find "$HOME/.config/openchamber" -type f 2>/dev/null | wc -l) files"
          else
            echo "No OpenChamber config data to save"
          fi

          if [ -d "$HOME/.config/opencode-worktree" ] && [ "$(ls -A "$HOME/.config/opencode-worktree" 2>/dev/null)" ]; then
            cp -rv "$HOME/.config/opencode-worktree/"* /tmp/opencode-save/opencode-worktree/ 2>/dev/null || true
            echo "Worktree config saved: $(find "$HOME/.config/opencode-worktree" -type f 2>/dev/null | wc -l) files"
          fi

          if [ -f "$HOME/.local/share/opencode/auth.json" ]; then
            cp -v "$HOME/.local/share/opencode/auth.json" /tmp/opencode-save/auth.json 2>/dev/null || true
            echo "Auth data saved"
          fi

          if [ -f "$HOME/.local/share/opencode/opencode.db" ]; then
            cp -v "$HOME/.local/share/opencode/opencode.db"* /tmp/opencode-save/ 2>/dev/null || true
            echo "SQLite database saved: $(ls -la /tmp/opencode-save/opencode.db* 2>/dev/null | wc -l) files"
          fi

          if [ -d "$HOME/.local/state/opencode" ] && [ "$(ls -A "$HOME/.local/state/opencode" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/state/opencode/"* /tmp/opencode-save/state/ 2>/dev/null || true
            echo "State data saved: $(find "$HOME/.local/state/opencode" -type f 2>/dev/null | wc -l) files"
          fi

          if [ -d "$HOME/.local/share/opencode/project" ] && [ "$(ls -A "$HOME/.local/share/opencode/project" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/share/opencode/project/"* /tmp/opencode-save/project/ 2>/dev/null || true
            echo "Project sessions saved: $(find "$HOME/.local/share/opencode/project" -type f 2>/dev/null | wc -l) files"
          else
            echo "No project session data to save"
          fi

          if [ -d "$HOME/.local/share/opencode/worktree" ] && [ "$(ls -A "$HOME/.local/share/opencode/worktree" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/share/opencode/worktree/"* /tmp/opencode-save/worktree/ 2>/dev/null || true
            echo "Worktrees saved: $(find "$HOME/.local/share/opencode/worktree" -type f 2>/dev/null | wc -l) files"
          else
            echo "No worktree data to save"
          fi

          if [ -d "$HOME/.local/share/opencode/storage" ] && [ "$(ls -A "$HOME/.local/share/opencode/storage" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/share/opencode/storage/"* /tmp/opencode-save/storage/ 2>/dev/null || true
            echo "Legacy storage saved: $(find "$HOME/.local/share/opencode/storage" -type f 2>/dev/null | wc -l) files"
          fi

          if [ -d "$HOME/.cache/opencode" ] && [ "$(ls -A "$HOME/.cache/opencode" 2>/dev/null)" ]; then
            cp -rv "$HOME/.cache/opencode/"* /tmp/opencode-save/cache/ 2>/dev/null || true
            echo "Cache saved: $(find "$HOME/.cache/opencode" -type f 2>/dev/null | wc -l) files"
          fi

          if [ -d "$HOME/.local/share/opencode/log" ] && [ "$(ls -A "$HOME/.local/share/opencode/log" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/share/opencode/log/"* /tmp/opencode-save/log/ 2>/dev/null || true
            echo "Logs saved: $(find "$HOME/.local/share/opencode/log" -type f 2>/dev/null | wc -l) files"
          fi

          if [ -d "$HOME/.local/share/opencode/bin" ] && [ "$(ls -A "$HOME/.local/share/opencode/bin" 2>/dev/null)" ]; then
            cp -rv "$HOME/.local/share/opencode/bin/"* /tmp/opencode-save/bin/ 2>/dev/null || true
            echo "Bin directory saved: $(find "$HOME/.local/share/opencode/bin" -type f 2>/dev/null | wc -l) files"
          fi

          if [ "$(find /tmp/opencode-save -type f 2>/dev/null | wc -l)" -gt 0 ]; then
            echo "Session data prepared successfully: $(find /tmp/opencode-save -type f | wc -l) files total"
            ls -la /tmp/opencode-save/
          else
            echo "No session data to save - creating placeholder"
            echo "$(date): No session data available" > /tmp/opencode-save/placeholder.txt
          fi

      - name: Save Oh My OpenAgent Data
        if: always()
        run: |
          set -euo pipefail
          echo "Saving Oh My OpenAgent / Oh My OpenCode files..."
          mkdir -p /tmp/opencode-save/opencode-omo-config \
                   /tmp/opencode-save/omo-config-openagent \
                   /tmp/opencode-save/omo-config-opencode \
                   /tmp/opencode-save/omo-share-openagent \
                   /tmp/opencode-save/omo-share-opencode \
                   /tmp/opencode-save/omo-cache-openagent \
                   /tmp/opencode-save/omo-cache-opencode \
                   /tmp/opencode-save/omo-home-openagent \
                   /tmp/opencode-save/omo-home-opencode \
                   /tmp/opencode-save/repo-omo-configs

          find "$HOME/.config/opencode" -maxdepth 1 -type f \( \
            -name 'oh-my-openagent.json' -o -name 'oh-my-openagent.jsonc' -o \
            -name 'oh-my-opencode.json' -o -name 'oh-my-opencode.jsonc' \
          \) -exec cp -v {} /tmp/opencode-save/opencode-omo-config/ \; 2>/dev/null || true

          [ -d "$HOME/.config/oh-my-openagent" ] && cp -rv "$HOME/.config/oh-my-openagent/"* /tmp/opencode-save/omo-config-openagent/ 2>/dev/null || true
          [ -d "$HOME/.config/oh-my-opencode" ] && cp -rv "$HOME/.config/oh-my-opencode/"* /tmp/opencode-save/omo-config-opencode/ 2>/dev/null || true
          [ -d "$HOME/.local/share/oh-my-openagent" ] && cp -rv "$HOME/.local/share/oh-my-openagent/"* /tmp/opencode-save/omo-share-openagent/ 2>/dev/null || true
          [ -d "$HOME/.local/share/oh-my-opencode" ] && cp -rv "$HOME/.local/share/oh-my-opencode/"* /tmp/opencode-save/omo-share-opencode/ 2>/dev/null || true
          [ -d "$HOME/.cache/oh-my-openagent" ] && cp -rv "$HOME/.cache/oh-my-openagent/"* /tmp/opencode-save/omo-cache-openagent/ 2>/dev/null || true
          [ -d "$HOME/.cache/oh-my-opencode" ] && cp -rv "$HOME/.cache/oh-my-opencode/"* /tmp/opencode-save/omo-cache-opencode/ 2>/dev/null || true
          [ -d "$HOME/.oh-my-openagent" ] && cp -rv "$HOME/.oh-my-openagent/"* /tmp/opencode-save/omo-home-openagent/ 2>/dev/null || true
          [ -d "$HOME/.oh-my-opencode" ] && cp -rv "$HOME/.oh-my-opencode/"* /tmp/opencode-save/omo-home-opencode/ 2>/dev/null || true

          if [ -d "$HOME/repos" ]; then
            (cd "$HOME/repos" && find . -type f \( \
              -path '*/.opencode/oh-my-openagent.json' -o \
              -path '*/.opencode/oh-my-openagent.jsonc' -o \
              -path '*/.opencode/oh-my-opencode.json' -o \
              -path '*/.opencode/oh-my-opencode.jsonc' \
            \) -print0 | tar --null -T - -cf -) | (cd /tmp/opencode-save/repo-omo-configs && tar -xf -) 2>/dev/null || true
          fi

      - name: Upload Session Data (Artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opencode-session
          path: /tmp/opencode-save/
          retention-days: 30
          overwrite: true
          if-no-files-found: warn
